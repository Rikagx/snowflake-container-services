manifest_version: 1

version:
  name: V0
  label: Example Version

artifacts:
  setup_script: setup.sql
  container_services:
    images:
    - /pro/app/img/workbench:latest

privileges:
  - BIND SERVICE ENDPOINT:
      description: "a service can serve requests from public endpoint"
      